<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Web</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Welcome to My Blog</h1>
        
        
        <% if (posts.length === 0) { %>
            <p>No posts available. Start by creating your first post!</p>
        <% } else { %>
            <ul>   
                <% posts.forEach(post => { %>
                    <li>
                        <h2><a href="/post/<%= post.id %>"><%= post.title %></a></h2>
                        <p>By <%= post.author %></p>
                        
                        <div class="post-actions">
                            <a href="/edit/<%= post.id %>">✏️ Edit</a>
                            <form method="POST" action="/delete/<%= post.id %>" style="display:inline;">
                                <button type="submit">🗑️ Delete</button>
                            </form>
                        </div>
                    </li>
                <% }) %>
            </ul>
        <% } %>
        <a href="/new" class="create-btn">✍️ Create New Post</a>
    </div>
</body>
</html>
